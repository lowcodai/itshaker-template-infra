# Ansible Inventory — placeholder
# Replace with your actual inventory or use a dynamic inventory.
all:
  hosts: {}
  children:
    production:
      hosts: {}
    staging:
      hosts: {}
    development:
      hosts: {}
