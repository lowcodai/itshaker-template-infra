# Ansible Inventory — placeholder
# Remplacer par votre inventaire réel ou utiliser un inventaire dynamique.
all:
  hosts: {}
  children:
    production:
      hosts: {}
    staging:
      hosts: {}
    development:
      hosts: {}
